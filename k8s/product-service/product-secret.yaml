# product-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: product-secret
  namespace: product-service # Ensure this matches the namespace of your deployment
type: Opaque
data:
  # Placeholder only: replace in your CI or secret manager. Do NOT commit real credentials.
  # Example to produce a base64 value: echo -n 'postgrespassword' | base64
  POSTGRES_PASSWORD: "<base64-POSTGRES_PASSWORD-PLACEHOLDER>"